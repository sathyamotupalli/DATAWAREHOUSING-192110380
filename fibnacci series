# Function to generate the nth Fibonacci number
fibonacci <- function(n) {
  if (n <= 0) {
    return(0)
@@ -9,11 +8,9 @@ fibonacci <- function(n) {
  }
}

# Generate the first 10 Fibonacci numbers
numbers <- c()
for (i in 1:10) {
  numbers <- c(numbers, fibonacci(i))
}

# Print the first 10 Fibonacci numbers
print(numbers)
